<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Visibility only for the owner-selected, optional public-source browser handoff. -->
    <queries>
        <!-- Visibility only, not a microphone grant or service activation. -->
        <intent><action android:name="android.speech.RecognitionService" /></intent>
        <package android:name="com.android.chrome" />
        <package android:name="com.brave.browser" />
        <package android:name="com.microsoft.emmx" />
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Maya"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Standalone native text UI: ignores inbound data/extras; no deep links. -->
        <activity android:name=".chat.NativeChatActivity" android:exported="false"
            android:label="Maya Text Chat" android:windowSoftInputMode="adjustResize">
        </activity>

        <!-- Separate read-only research Agent. Incoming data/extras are not commands. -->
        <activity android:name=".agent.ResearchActivity" android:exported="false"
            android:label="Maya Agent" android:windowSoftInputMode="adjustResize">
        </activity>
        <activity android:name=".agent.AgentActivity" android:exported="false"
            android:label="Maya Agent Lab" android:windowSoftInputMode="adjustResize" />

        <!-- Explicit native recovery entry. Ignores incoming URLs and extras. -->
        <activity android:name=".update.UpdateActivity" android:exported="true"
            android:label="MAYA Updates">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider android:name=".update.UpdateFileProvider"
            android:authorities="${applicationId}.updates" android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/update_file_paths" />
        </provider>

        <service
            android:name=".WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".AutoSendService" android:enabled="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/autosend_service_config" />
        </service>

        <service
            android:name=".MayaNotifService" android:enabled="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".ScheduledReceiver" android:enabled="false"
            android:exported="false" />

        <!-- 🛑 Roadmap Phase 1: kill-switch button receiver -->
        <receiver
            android:name=".MayaStopReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".BootReceiver" android:enabled="false"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
