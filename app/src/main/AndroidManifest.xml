<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- ═══ 🗣️ v5.10.1 — PACKAGE VISIBILITY (Android 11+) ═══
         Pehle MAYA ko Gboard / Google app / Speech-by-Google NAZAR hi nahi aate
         the (Android 11 se har app doosri app ko khud nahi dekh sakti). Isi liye:
           • 🩺 KAAN DOCTOR hamesha "Speech by Google app: nahi" kehta tha
             (getApplicationInfo NameNotFoundException phenk deta tha) — CHAHE
             app install ho. Ek jhoot jo user ko galat raaste par bhejta tha.
           • 🗣️ ON-DEVICE LANGUAGE ka pehla qadam (Gboard VoiceSettingsActivity)
             hamesha fail hota tha, aur chain agli screen par gir jati thi.
         Ye <queries> koi permission NAHI maangta — sirf "mujhe ye naam dekhne
         hain" batata hai. RecognitionService wala intent-filter Android khud
         visible rakhta hai, is liye speech services ki fehrist pehle se chalti thi. -->
    <queries>
        <package android:name="com.google.android.inputmethod.latin" />
        <package android:name="com.google.android.inputmethod.latin.go" />
        <package android:name="com.google.android.googlequicksearchbox" />
        <package android:name="com.google.android.apps.searchlite" />
        <package android:name="com.google.android.tts" />
        <package android:name="com.google.android.ai.aicore" />
        <package android:name="com.google.android.as" />
        <!-- Play Store: "market:" darwaza (Speech Services by Google install karne ka) -->
        <package android:name="com.android.vending" />
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
        <intent>
            <action android:name="android.intent.action.ASSIST" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent>
        <!-- Settings ke wo darwaze jo go() POOCHHTA hai (warna resolve hi nahi hote) -->
        <intent>
            <action android:name="android.settings.VOICE_INPUT_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.INPUT_METHOD_SETTINGS" />
        </intent>
        <!-- "Digital assistant" wali screen: Settings > Apps > Default apps
             (AOSP ka asal action — public constant is ka hai hi nahi) -->
        <intent>
            <action android:name="android.settings.MANAGE_DEFAULT_APPS_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.settings.IGNORE_BATTERY_OPTIMIZATION_SETTINGS" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="package" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="market" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.SETTINGS" />
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.Maya"
        android:allowBackup="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".WakeWordService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".AutoSendService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/autosend_service_config" />
        </service>

        <service
            android:name=".MayaNotifService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <receiver
            android:name=".ScheduledReceiver"
            android:exported="false" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
